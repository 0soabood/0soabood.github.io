# Session Notes — 2026-05-03

## What was accomplished
- Set up Docusaurus 3.10.1 personal blog from scratch
- Configured blog-only mode (no docs, blog serves from root `/`)
- Fixed deployment config: corrected GitHub Pages organization and project name
- Added `trailingSlash: false` to suppress deploy warnings
- Wrote and published "Hello, World" introductory post
- Enabled system-preference dark mode

## Technologies touched
- Docusaurus 3.10.1, React 19, MDX, GitHub Pages

## Interesting notes
- The scaffold defaults to `Lorchissimo` as org name — had to correct to `0soabood`
- Docusaurus `blog-only` mode requires setting `docs: false` and `routeBasePath: '/'` in the blog plugin config
- `trailingSlash: false` is needed explicitly for GitHub Pages to avoid redirect loops

## Next session ideas
- Write about the Docusaurus setup process itself
- Add more tags to tags.yml as topics emerge
